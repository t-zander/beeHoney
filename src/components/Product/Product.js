import React from "react";
import "./Product.scss";

const Product = props => {
  const { product } = props;

  return (
    <div className="product">
      Product
    </div>
  );
};

export default Product;
